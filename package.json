{
  "name": "genius-yield-fill-bot",
  "version": "0.1.0",
  "description": "Reference TypeScript bot that fills GeniusYield limit orders by arbing against external AMMs. Open-sourced as the integration example for the v1 order-book / fill-quote / fill-submit APIs.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "smoke": "DRY_RUN=true node dist/index.js"
  },
  "dependencies": {
    "@lucid-evolution/lucid": "^0.4.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=20"
  }
}
